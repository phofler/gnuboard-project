# 내일 할 일 (To-Do List)

## 1. 메인 컨텐츠 Style C 레이아웃 정렬 수정 (최우선)
*   **현상:** Style C (Vertical Slide)의 컨텐츠 폭이 Style A보다 넓게 퍼져 보임 (Container 영역을 벗어나는 느낌).
*   **목표:** Style A와 완벽하게 동일한 좌우 여백 및 정렬(Inside Alignment) 맞추기.
*   **점검 예정:**
    1.  `div.container` 공통 클래스가 제대로 적용되었는지 확인.
    2.  `padding` 값이 Style A와 동일한지 비교.
    3.  `.style-c` 내부의 불필요한 `margin`이나 `width: 100%` 속성이 부모 패딩을 무시하는지 체크.

## 2. 상단 메뉴 (GNB) CSS 문제 해결 (Basic Light 스킨)
*   **현상:** `default.css` 수정에도 불구하고 스타일 깨짐 또는 미반영.
*   **체크리스트:**
    *   브라우저 캐시 및 CSS 우선순위(Specificity) 확인.
    *   JS에 의한 `display` 속성(`block` vs `flex`) 충돌 확인.

## 3. 온라인 문의 (Online Inquiry) 스킨 점검
*   다크 테마 환경에서 입력 폼 및 버튼 디자인 확인.
*   모바일 반응형 동작 점검.

## 4. 전체 레이아웃 안정화
*   서브 페이지 Hero 섹션과 Breadcrumb 겹침 현상 확인.

---
**메모:** Style C 작업 시 `main_content.lib.php`의 CSS를 중점적으로 확인하세요.
