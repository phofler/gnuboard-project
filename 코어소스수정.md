# 코어 소스 수정 내역

## 2025-12-19
### 1. 관리자 토큰 처리 로직 수정 (CSRF 방지 강화 및 오류 해결)
**목적**: 관리자 페이지(특히 플러그인 포함)에서 폼 전송 시 토큰 재발급으로 인한 "올바른 방법으로 이용해 주세요" 오류 해결 및 보안 강화.

#### `adm/admin.js`
- **위치**: `submit` 이벤트 핸들러 내부 (약 223라인)
- **수정 내용**: 폼에 이미 유효한 `token`이 있는 경우, 불필요하게 `ajax.token.php`를 호출하여 토큰을 갱신하지 않고 기존 값을 유지하도록 변경.
```javascript
// 이미 토큰 값이 존재하는 경우 새로 생성하거나 덮어쓰지 않음
if ($token.length > 0 && $token.val() !== "") {
    return true;
}
```

#### `adm/ajax.token.php`
- **위치**: 파일 상단
- **수정 내용**: 클라이언트(`admin.js`)로부터 전달받은 `admin_csrf_token_key`가 서버의 키와 일치하는지 검증하는 로직 추가. 비정상적인 경로의 토큰 발급 요청 차단.
```php
$admin_csrf_token_key = isset($_POST['admin_csrf_token_key']) ? $_POST['admin_csrf_token_key'] : '';

if (function_exists('admin_csrf_token_key') && $admin_csrf_token_key !== admin_csrf_token_key(1)) {
    $debug_msg = 'KeyMismatch: Post[' . $admin_csrf_token_key . '] vs Server[' . admin_csrf_token_key(1) . ']';
    die(json_encode(array('error' => $debug_msg, 'url' => G5_URL)));
}
```

---
**백업 파일**:
- `adm/admin.js.20251219.bak`
- `adm/ajax.token.php.20251219.bak`

## 2026-01-04
### 1. 스마트 에디터 스킨 캐시 갱신 (배경색 미반영 해결)
**목적**: 스마트 에디터가 브라우저 캐시로 인해 구버전 스킨(`SmartEditor2Skin.html`)을 불러와, 테마 및 배경색 설정 파라미터가 전달되지 않는 문제 해결.

#### `plugin/editor/smarteditor2/config.js`
- **위치**: `nhn.husky.EZCreator.createInIFrame` 설정 내부 (약 11라인)
- **수정 내용**: `sSkinURI` 경로 뒤에 타임스탬프(`?ver=[Time]`)를 붙여 매 로딩 시 강제로 최신 파일을 불러오도록 변경 (Cache Busting).
```javascript
// [변경 전]
sSkinURI: g5_editor_url+"/SmartEditor2Skin.html",

// [변경 후]
sSkinURI: g5_editor_url+"/SmartEditor2Skin.html?ver=" + new Date().getTime(),
```

## 2026-01-13
### 1. 게시판 메일 발송 로직 리팩토링 및 코어 복구
**목적**: `Policy 13 (코어 수정 최소화)`에 따라 `bbs/write_update.php`에 직접 삽입되었던 커스텀 로직을 제거하고, 이를 확장 훅(`extend`) 및 테마 스킨으로 완전히 분리.

#### `bbs/write_update.php` [복구]
- **수정 내용**: `chamcode_gallery` 게시판의 메일 발송을 강제로 제어하던 커스텀 코드 블록을 삭제하여 순정 상태로 복구.

#### `extend/online_mail_hook.extend.php` [수정]
- **수정 내용**: `chamcode_gallery` 게시판에서 확장 훅에 의해 메일이 중복 발송되는 것을 방지하기 위해 제외 로직(`if ($board['bo_table'] == 'chamcode_gallery') return;`) 활성화.

#### `theme/corporate_light/skin/board/gallery_editorial/write_update.tail.skin.php` [수정]
- **수정 내용**: 글 등록/수정 완료 후 리다이렉트 URL 파라미터에 `me_code`를 유지하고, 리스트 페이지의 카테고리 영역(`#bo_cate`)으로 자동 스크롤되도록 앵커 추가.

#### `theme/corporate_light/skin/board/gallery_editorial/list.skin.php` [수정]
- **수정 내용**: 카테고리 탭 영역 상단에 앵커 ID(`<div id="bo_cate" ...>`) 추가.

---
**백업 파일**:
- `bbs/write_update.php.20260113.bak` (작업 전 복구 포인트)

