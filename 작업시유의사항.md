
# 작업 시 유의사항 (Project Guidelines)

## 1. CSS 파일 구조 및 관리 규칙 (Mandatory)
**작업 전 필수 확인: 모든 CSS 작업은 아래 구조에 맞춰 해당 파일에만 수행할 것.**
한 파일(`default.css`)에 모든 코드를 몰아넣는 것을 금지하며, 기능별로 분리하여 관리한다.

### CSS 파일 구성 (4-File Structure)
1.  **`theme/corporate/css/default.css`**
    *   **역할:** 공통 뼈대 및 메인 스타일
    *   **포함:** CSS Reset, 공통 변수(:root), 폰트, 전체 레이아웃 틀, 푸터(Footer), 메인 페이지 전용 스타일
2.  **`theme/corporate/css/menu_top.css`**
    *   **역할:** 상단 헤더 및 메뉴 전용
    *   **포함:** GNB(상단메뉴), 전용 모바일 메뉴, 상단바 디자인
3.  **`theme/corporate/css/sub_common.css`** (구 `sub_img`)
    *   **역할:** 서브 페이지 공통 요소
    *   **포함:** 서브 비주얼(Hero 이미지), 페이지 타이틀, 네비게이션(Breadcrumb), 서브 공통 여백
4.  **`theme/corporate/css/sub_style.css`**
    *   **역할:** 서브 페이지 개별 콘텐츠
    *   **포함:** 회사소개 본문, 표(Table), 폼(Form), 버튼, 기타 콘텐츠 내부 스타일

### 작업 원칙
*   CSS 수정 시 **무조건 전체 파일을 읽지 말고**, 수정하려는 섹션이 포함된 **전용 파일만** 찾아서 작업한다.
*   파일을 합치거나 덮어쓸 때(`write_to_file`)는 **기존 데이터 유실에 각별히 유의**한다. (가능하면 `replace`나 `append` 사용)

---

## 2. 기존 유의사항 (Legacy)
*   **파일 수정 전 백업 필수:** 중요한 파일 수정 시 반드시 백업본을 확인하거나 생성 후 진행.
*   **그누보드 테마 규칙 준수:** `head.sub.php` 등 코어 파일을 건드릴 때는 테마 로딩 순서를 고려할 것.

---

## 3. 서브 비주얼(Hero) 작업 규칙 (Critical)
**배경:** 2025-12-17 이슈 해결 (이중 구조 제거 및 이미지 우선순위 정립)

### A. 단일 소스 원칙 (Single Source of Truth)
*   **원칙:** 서브 페이지 상단 비주얼(`Hero Section`)은 **오직 `theme/corporate/head.php` 한 곳에서만 생성**한다.
*   **금지:** 개별 페이지(예: `premium.html`)에서 똑같은 비주얼 영역을 중복해서 만들지 않는다.
*   **구조:** `head.php`가 관리자 설정값을 받아 표준화된 클래스 `section.sub-hero`를 출력하고, CSS는 `sub_common.css`가 담당한다.

### B. 이미지 로딩 우선순위 (Priority Logic)
`head.php`는 다음 순서대로 이미지를 결정해야 한다. (안전장치가 URL을 덮어쓰지 않도록 주의)
1.  **1순위 (Admin File):** 관리자가 업로드한 파일이 있으면 무조건 최우선 사용.
2.  **2순위 (Admin URL):** 파일이 없고 외부 이미지 URL(`Unsplash` 등)이 입력되어 있으면 사용.
3.  **3순위 (Fallback):** 위 두 가지가 모두 없을 때만 테마 기본 이미지 사용.


---

## 4. 모듈화 가이드라인 (Refactoring Lessons) - 2025.12.17
**시스템의 유지보수성과 확장성을 위해 반드시 지켜야 할 원칙입니다.**

### A. 역할 분리 (Separation of Concerns)
*   **테마(Theme):**
    *   **담당:** 사이트 전체 레이아웃, 공통 네비게이션(Breadcrumb), 공통 디자인 스타일.
    *   **파일:** `head.php`, `tail.php`, `css/sub_common.css`
*   **플러그인(Plugin):**
    *   **담당:** 해당 기능이 보여줘야 할 핵심 본문(Contents) 데이터 출력.
    *   **파일:** `view.php` (순수 데이터 출력만 담당해야 함)
    *   **금지:** 플러그인 파일에서 자바스크립트로 부모 요소(`body`, `#wrapper` 등)의 스타일이나 위치를 강제로 조작하는 행위 금지.

### B. CSS API 패턴 활용
*   플러그인이 테마의 스타일(예: 배경색)을 제어해야 할 경우, 직접 스타일을 덮어쓰지 말고 **규속된 변수 시스템**을 활용하거나, 테마가 제공하는 **클래스 스위치**를 사용해야 한다. (이번 사례에서는 아예 제거하여 테마 기본값인 투명을 활용함)


## 6. 자바스크립트 모듈화 및 위치 규칙 (Script Modularity) - 2025.12.18
**웹 성능 최적화와 안정적인 DOM 제어를 위한 규칙입니다.**

### A. 스크립트 위치 원칙 (Script Placement)
*   **원칙:** 모든 공통 자바스크립트 라이브러리(AOS, Slider 등) 및 초기화 스크립트는 **`tail.sub.php` (페이지 최하단)**에 배치한다.
*   **이유:**
    1.  **성능:** 렌더링 차단 방지 (화면이 먼저 뜨고 스크립트 실행).
    2.  **안정성:** 모든 DOM 요소가 생성된 후 실행되므로 `null` 참조 오류 방지.
*   **금지:** `head.php`나 `head.sub.php`에 무거운 스크립트를 직접 넣는 것을 지양한다. (CSS와 필수 메타태그만 유지)

### B. 중복 제거 (DRY)
*   개별 플러그인(`view.php`)이나 스킨(`content.skin.php`)에서 라이브러리를 중복 로드(`script src=...`)하지 않는다.
*   테마(`tail.sub.php`)에서 한 번만 로드하고, 개별 파일은 순수 HTML/Data 출력에만 집중한다.

## 5. 작업 로그 요약 (2025-12-17)

### 수정 파일 리스트
1.  **`theme/corporate/head.php`**: 네비바(Breadcrumb) 로직 이식 (플러그인 의존성 제거 및 전체 적용)
2.  **`theme/corporate/css/sub_common.css`**: 네비바 스타일 통합, 노란색 라인 변수 수정
3.  **`theme/corporate/css/default.css`**: `#wrapper` 배경색 제거 (투명화로 디자인 버그 해결)
4.  **`plugin/company_intro/view.php`**: JS/HTML 제거 및 경량화 (본문 전담)

## 7. 스킨 개발 및 확장 규칙 (Skin Development Policy) - 2025.12.18
**확장성을 고려한 다중 스킨 관리 원칙입니다.**

### A. 독립형 스킨 구조 (Self-Contained)
*   **원칙:** 새로운 스킨(예: `type_a_1.html`)을 추가할 때는 **HTML 구조와 전용 CSS(`style` 태그)를 하나의 파일에 모두 포함**시킨다.
*   **이유:**
    1.  **확장성:** 스킨이 수십 개로 늘어나더라도 글로벌 CSS 파일(`sub_style.css`)이 비대해지는 것을 방지한다.
    2.  **이식성:** 파일 하나만 복사하면 스킨이 즉시 작동하며, 관리자 에디터(SmartEditor2)에서도 별도 조치 없이 디자인이 바로 적용된다.
*   **예외:** 모든 스킨에서 공통으로 반드시 써야 하는 아주 기본적인 레이아웃(예: `.content-wrapper`) 정도만 글로벌 CSS에 둔다.

## 8. 스킨 CSS 및 캐시 처리 정책 (Specificity & Caching) - 2025.12.18
**프론트엔드 스타일 충돌 및 관리자 캐시 문제를 방지하기 위한 필수 지침입니다.**

### A. CSS 우선순위 해결 (Class Strategy)
*   **문제:** 스킨의 텍스트 색상이 테마의 기본 스타일(예: `body p`)에 밀려 적용되지 않는 경우 발생.
*   **해결:**
    1.  상속(`Inheritance`)에 의존하지 않는다. (예: 부모 div에만 color 지정 금지)
    2.  `!important` 사용을 지양한다.
    3.  **원칙:** **반드시 대상 요소에 전용 클래스를 부여**하여 직접 타겟팅한다. (예: `<p class="intro-text">` → `.intro-text { color: ... }`)

### B. 관리자 스킨 로딩 (Cache Busting)
*   **문제:** 관리자 페이지에서 스킨 로드(`$.ajax`) 시 브라우저가 강력하게 캐싱하여 수정 사항이 반영되지 않음.
*   **해결:** URL 뒤에 타임스탬프(`Date.getTime()`)보다 강력한 **`Math.random()`** 을 파라미터로 붙여 매 요청마다 새로운 파일을 가져오도록 강제한다.

## 9. 플러그인 출력 및 설정 연동 규칙 (View Responsibility) - 2025.12.18
**관리자 설정값이 누락되지 않도록 하기 위한 뷰(View) 파일 작성 원칙입니다.**

### A. 배경색 및 컨테이너 스타일 처리
*   **문제:** 스킨 HTML 파일에만 몰두하다가, DB에 저장된 **배경색(`co_bgcolor`)이나 여백 설정**을 출력하는 코드를 누락하는 실수 발생.
*   **원칙:** **스킨 파일(`type_*.html`)은 내부 콘텐츠** 구조만 담당하고, **뷰 파일(`view.php`)이 전체를 감싸는 래퍼(Wrapper)** 역활을 수행하며 DB 설정을 반영해야 한다.
*   **예시:**
    ```php
    // view.php
    $bg_color = $co_row['co_bgcolor'] ? $co_row['co_bgcolor'] : 'transparent';
    echo '<div style="background-color:'.$bg_color.'">'; // 래퍼에서 배경색 처리
    echo get_skin_content(...); // 스킨 본문 출력
    echo '</div>';
    ```

## 10. 상단 메뉴 CSS 관리 규칙 (Top Menu Strategy) - 2025.12.18
**이중 구조(Dual-Track) 전략: 기본 테마와 확장 플러그인 스타일의 공존 원칙**

1.  **`theme/.../menu_top.css` (테마 내장)**
    *   **역할:** 플러그인 미사용 시 작동하는 **순정 GNB 스타일**.
    *   **규칙:** 이곳의 코드는 플러그인 스킨 디자인에 간섭하지 않도록 최소한으로 유지한다.

2.  **`plugin/.../skins/스킨명/style.css` (플러그인 모드)**
    *   **역할:** 특정 디자인(Modern, Dark 등)을 구현하는 **독립 스타일**.
    *   **규칙:**
        *   기존 CSS 파일을 건드리지 않고, 해당 스킨 파일(`style.css`) 내에서만 작업한다.
        *   **Prefix 필수:** `.gnb_list` 같은 흔한 이름 대신 `.modern-gnb`, `.dark-menu` 같은 **고유 접두어**를 사용하여 테마 CSS와의 충돌을 원천 차단한다.

## 11. 관리자 토큰/세션 오류 해결 가이드 (Troubleshooting) - 2025.12.19
**관리자 페이지에서 '토큰 정보가 올바르지 않습니다' 오류 발생 시 대응 매뉴얼입니다.**

### A. 증상 및 원인
*   **증상:** 게시판 설정 저장, 스킨 적용 등 폼 전송 시 `ajax.token.php` 오류나 `KeyMismatch` 경고창이 뜨며 동작하지 않음.
*   **원인:** 브라우저에 남은 토큰 키와 서버의 세션 정보가 불일치하거나, 세션이 만료된 상태에서 요청을 보냈기 때문.

### B. 해결 방법
1.  **세션파일 일괄삭제 (권장):**
    *   관리자 페이지 좌측 메뉴(환경설정 그룹)에 있는 **[세션파일 일괄삭제]** 버튼을 클릭한다.
    *   이 기능은 서버에 남아있는 꼬인 세션 파일들을 일괄 삭제하여 초기화한다.
2.  **재로그인:**

## 12. 상단 메뉴 스킨 작업 시 필수 유의사항 (Top Menu Skin Precautions) - 2025.12.19
**테마 레이아웃과 플러그인 스킨 간의 충돌 방지를 위한 핵심 기술 지침입니다.**

### A. 테마 래퍼(Wrapper) 충돌 해결
*   **증상:** 상단 메뉴 스킨(Mega Menu 등)을 넓게(예: 1600px) 만들어도, 테마 헤더의 컨테이너(`div#hd_wrapper`)가 **1400px** 등으로 제한되어 있으면 메뉴가 찌그러지거나 정렬이 틀어짐.
*   **해결:**
    1.  **전체 너비가 필요한 스킨**의 경우, 테마 파일(`head.php`)에서 **`div#hd_wrapper` 태그를 주석 처리하거나 제거**해야 한다.
    2.  스킨 파일(`menu.skin.php`) 내부에서 **직접 `div.gnb_wrap` 등을 최상위**에 배치하여 레이아웃 너비를 제어한다.
    3.  CSS 오버라이드(`!important`) 방식보다 **HTML 구조 자체를 상위(Top Level)로 올리는 것**이 가장 확실한 해결책이다.

### B. 샘플 디자인 이식 원칙
*   사용자가 제공한 샘플 파일(`menu_mega.html`)이 있을 경우, 기존 스킨 CSS에 끼워 맞추려 하지 말고 **샘플 파일의 HTML 구조와 CSS를 그대로 복제하여 이식**하는 것이 가장 정확하다.


## 13. �ھ� �ҽ� ���� ��å (Core Modification Policy) - 2025.12.19
**�״����� ���� �ھ� ����(\dm\, \lib\, \bs\ ��)�� �����ؾ� �� ��� �ݵ�� ���� �����Դϴ�.**

### A. ��Ģ (Principles)
1.  **���� ����:** ������ �� ��ŷ(\hook\), ��Ų, �׸� ����, �÷����� �� **���ı����� ���**�� �켱 �����Ѵ�.
2.  **�ּ� ����:** �Ұ����� ���, ������ �ذ��ϴ� **���� �ּ����� ����**�� �ڵ带 �����Ѵ�.

### B. �۾� ���� (Workflow)
1.  **���� ��� (Backup Original):**
    *   �����ϱ� ������ ������ �ƴ϶�, **�������� ���� ������ ���� ����**�� ������ ����Ѵ�.
    *   ���ϸ� ����: \[���ϸ�].YYYYMMDD.bak\ (��: \dm/admin.js.20251219.bak\)
    *   �� ��� ������ ���� ����(Rollback)�̳� ���� �߻� �� ������(\diff\)�� �ȴ�.
2.  **���� ��� (Documentation):**
    *   \�ھ�ҽ�����.md\ ���Ͽ� ���� ������ �ݵ�� �߰��Ѵ�.
    *   **��� �׸�:** ��¥, ���� ���ϸ�, ���� ����, ���� ����(�ڵ� ������), ��� ���ϸ�.
3.  **���� (Verification):**
    *   ���� �� ���̵� ����Ʈ�� ������ ö���� �׽�Ʈ�Ѵ�.

### C. �ھ� �ҽ� ���� �α� ��� (Log Format)
���� ������ �Ʒ� ������ �ؼ��Ͽ� \�ھ�ҽ�����.md\�� ����Ѵ�.
\\\markdown
## YYYY-MM-DD
### 1. [�۾� ��Ī]
**����:** ...
**��� ����:** \���/���ϸ�\
**���� ����:**
- [����]
- (�ڵ� ����)
**��� ����:** \���/���ϸ�.YYYYMMDD.bak\
\\\


## 14. 관리자 레이어 팝업 최상위 노출 (Layer Modal Stacking Fix) - 2025.12.20
**복잡한 관리자 레이아웃(Sidebar/Header)에 의해 팝업이 가려지는 문제 해결.**

### A. 문제 상황
*   **증상:** z-index를 99999로 설정했음에도 불구하고, 레이어 팝업이 사이드바나 상단 메뉴 아래에 깔림.
*   **원인:** 팝업 HTML 코드가 특정 div 컨테이너(Stacking Context) 내부에 위치하여, 부모 요소의 z-index나 overflow:hidden 속성에 갇힘.

### B. 해결 방법 (Javascript)
*   페이지 로드 시($(document).ready), 팝업 요소를 **Javascript로 강제로 Body 태그 직계 자식으로 이동**시킴.
*   **코드 예시:**
    `javascript
    .ready(function() {
        body.append(#previewModal); // 팝업을 body 로 이동
        body.append(#unsplashModal);
    });
    `
*   **결과:** 팝업이 DOM 트리의 최상위에 위치하게 되어, position:fixed가 Viewport 기준으로 정상 작동하며 모든 방해 요소를 덮을 수 있음.

## 14. 관리자 레이어 팝업 최상위 노출 (Layer Modal Stacking Fix) - 2025.12.20
**복잡한 관리자 레이아웃(Sidebar/Header)에 의해 팝업이 가려지는 문제 해결.**

### A. 문제 상황
*   **증상:** `z-index`를 99999로 설정했음에도 불구하고, 레이어 팝업이 사이드바나 상단 메뉴 아래에 깔림.
*   **원인:** 팝업 HTML 코드가 특정 `div` 컨테이너(Stacking Context) 내부에 위치하여, 부모 요소의 `z-index`나 `overflow:hidden` 속성에 갇힘.

### B. 해결 방법 (Javascript)
*   페이지 로드 시(`$(document).ready`), 팝업 요소를 **Javascript로 강제로 Body 태그 직계 자식으로 이동**시킴.
*   **코드 예시:**
    ```javascript
    $(document).ready(function() {
        $("body").append($("#previewModal")); // 팝업을 body 로 이동
        $("body").append($("#unsplashModal"));
    });
    ```
*   **결과:** 팝업이 DOM 트리의 최상위에 위치하게 되어, `position:fixed`가 Viewport 기준으로 정상 작동하며 모든 방해 요소를 덮을 수 있음.

## 15. 디자인 퍼블리싱 가이드 (Design Publishing Rules) - 2025.12.24
**메인 비주얼 높이 설정 및 반응형 전략 (Hero Section Hybrid Height)**

### A. 하이브리드 높이 전략 (Hybrid Height Strategy)
단순 %나 vh만 사용하면 모바일에서 너무 길거나 짧아지는 문제가 있어, 화면 크기별로 단위를 다르게 적용합니다.

1.  **PC (1600px 초과):** height: 100vh (화면 꽉 채움)
2.  **노트북 (1024~1600px):** aspect-ratio: 16/9, height: auto (비율 유지)
3.  **모바일 (1024px 이하):** height: 65vh, min-height: 600px, display: block (높이 강제 보장)
    *   **주의:** 모바일에서 Swiper 내부 요소들이 높이를 상속받지 못하는 경우가 많으므로 .swiper-wrapper, .swiper-slide에 height: 100% !important를 꼭 추가해야 합니다.

### B. 화살표 중앙 정렬
반응형 높이 변화에도 화살표가 중앙에 오도록 flex 대신 absolute + transform을 사용합니다.
css
.swiper-button-next/prev { top: 50%; transform: translateY(-50%); }



## 16. 기업 테마 개선 및 기능 추가 내역 (Corporate Theme Improvements) - 2025.12.25
**온라인 문의, 지도, 하단 정보 관리 등 반응형 및 관리자 편의성 개선.**

### A. 온라인 문의 폼 반응형 (Online Inquiry)
*   **변경:** 기존 768px 기준이던 반응형 분기점을 1024px로 상향 조정.
*   **목적:** 태블릿 등 중간 크기 화면에서도 입력 필드(input, 	extarea)가 전체 너비를 사용하여 터치 및 입력 편의성 확보.
*   **적용:** default.css 및 style.css 내 @media (max-width: 1024px) 규칙 추가.

### B. 회사 위치 정보 연동 (Location Info)
*   **변경:** index.php의 Location 섹션에 하드코딩된 주소를 제거하고 Copyright Manager 플러그인 연동.
*   **데이터 소스:** 관리자 > 테마 관리 > [카피라이트 및 하단 정보 관리]의 주소, 전화번호 값 활용.
*   **표시 형식:** Label : Value / Label : Value 형태로 명칭과 내용 구분 표시.

### C. 지도 API 반응형 및 중심점 보정 (Map API)
*   **높이 조절:** 모바일(768px 이하)에서 지도 높이를 500px -> 350px로 축소.
*   **자동 보정:** 브라우저 창 크기 조절 시(
esize 이벤트), 지도의 중심점(setCenter)을 다시 잡아 마커가 항상 중앙에 오도록 JS 로직 추가 (네이버, 구글, 카카오 공통 적용).

### D. 관리자 UI 편의성 개선 (Admin UI)
*   **버튼 추가:** [카피라이트 및 하단 정보 관리] 페이지 상단 제목 옆에 '확인(저장)' 버튼 추가.
*   **스타일:** 스크롤 시 상단에 고정되는 .btn_fixed_top 클래스를 적용하여 긴 폼에서도 즉시 저장 가능하도록 개선.


## 17. 개발 서버 환경 (Development Server Environment) - 2025.12.27
**로컬 및 배포 서버 환경 사양 (호환성 주의)**
*   **OS:** Windows (Local) / Linux (Server)
*   **PHP:** 7.4 (※ cgi 사용 불가, 반드시 모듈 방식 또는 php-fpm 확인)
*   **Database:** MariaDB 10.0.x
*   **Charset:** UTF-8
*   **Web Server:** Apache2 / HTTP2.0 지원


## 18. 신규 작업 시 기존 소스 분석 원칙 (Analyze Existing Patterns) - 2025.12.28
**기존과 유사한 작업을 진행할 때는 반드시 기존 소스를 먼저 파악해야 합니다.**

### A. 원칙
*   새로운 기능을 개발하거나 기존 기능을 확장할 때, 프로젝트 내에 유사한 기능이 있는지 먼저 검색합니다.
*   유사 기능이 있다면 해당 **파일 구조(디렉토리, 파일명 규칙)**와 **소스 구조(함수 명명법, 로직 흐름, 아작스 패턴 등)**를 분석합니다.
*   **목적:** 프로젝트 전체의 일관성(Consistency)을 유지하고, 검증된 패턴을 재사용하여 안정성을 높이기 위함입니다.
*   **실행:** 신규 작업 전 grep 등을 통해 기존 코드를 참조했음을 명시하거나, 기존 패턴을 따랐음을 주석이나 커밋 메시지에 남깁니다.

## 19. 스킨 연동 구현 시 필수 유의사항 (Skin Integration Logic) - 2025.12.30
**기존 스킨 UI를 타 플러그인(Pro Menu 등)에서 재구현할 때의 스타일링 원칙입니다.**

### A. 문제 상황 (Problem)
*   **증상:** 기존 스킨(`top_menu_manager/skins/basic`)의 HTML 구조를 그대로 가져와서 출력했음에도, 글자 말줄임(`text-overflow`)이나 서브 메뉴 숨김(`display: none`) 처리가 동작하지 않아 레이아웃이 깨짐.
*   **원인:** 해당 기능들을 제어하는 **CSS 파일(`style.css`)이 로드되지 않았기 때문**. HTML 태그와 클래스명만 맞춘다고 스타일이 적용되는 것이 아님.

### B. 해결 원칙 (Solution)
*   **CSS 재작성 금지:** 스킨이 이미 가지고 있는 기능을 위해 `inline style`이나 별도 CSS를 새로 짜지 않는다.
*   **CSS 파일 로드:** 해당 스킨의 `style.css` 파일을 **`add_stylesheet` 함수를 통해 명시적으로 로드**해야 한다.
*   **코드 예시:**
    ```php
    // 설정된 스킨명 가져오기
    $skin_path = G5_PLUGIN_PATH . '/top_menu_manager/skins/' . $top_menu_skin;
    $skin_url = G5_PLUGIN_URL . '/top_menu_manager/skins/' . $top_menu_skin;
    
    // 스킨 CSS 로드 (핵심)
    if (file_exists($skin_path . '/style.css')) {
        add_stylesheet('<link rel="stylesheet" href="' . $skin_url . '/style.css">', 0);
    }
    ```

## 20. 데이터 소스 변경 시 스킨 유지보수 원칙 (Backend Adapter Pattern) - 2025.12.30
**데이터 출처나 로직이 변경될 때, 스킨(Frontend)을 훼손하지 않고 연동하는 핵심 원칙입니다.**

### A. 실패 사례 (Failure Case)
*   **상황:** 메뉴 데이터를 기존 1개 테이블에서 **2개 테이블(Pro Menu + Tree Category) 병합 방식**으로 변경함.
*   **실수:** 병합된 데이터 구조가 기존 스킨과 맞지 않자, **스킨의 CSS와 HTML 구조를 억지로 수정**하여 끼워 맞추려고 시도함.
*   **결과:** '스킨 교체 시 디자인이 자동 변경된다'는 핵심 기능이 파괴됨. 데이터가 바뀔 때마다 모든 스킨을 일일이 수정해야 하는 유지보수 지옥 발생.

### B. 해결 원칙 (Solution Principle)
1.  **백엔드 어댑터 (Backend Responsibility):**
    *   데이터 로직(`lib.php` 등)이 '통역사' 역할을 해야 한다.
    *   서로 다른 테이블에서 가져온 날것의 데이터(Raw Data)를 가공하여, **스킨이 원래 기대하던 '표준 데이터 포맷'으로 완벽하게 변환**한 후 넘겨줘야 한다.
2.  **프론트엔드 격리 (Frontend Isolation):**
    *   스킨 파일(`view.php`, `style.css`)은 데이터가 어디서 왔는지 알 필요가 없어야 한다.
    *   **절대 원칙:** 데이터 구조가 안 맞는다고 해서 **잘 돌아가던 스킨의 CSS나 HTML을 뜯어고치지 않는다.**

### C. OS 경로 호환성 (Cross-Platform Path)
*   **문제:** 로고나 CSS 파일 경로를 리눅스 스타일(`/`)로 하드코딩하여, 윈도우 로컬 개발환경(`\`)에서 엑박(404) 발생.

## 21. 상단 메뉴 시스템 아키텍처 및 데이터 구조 (Menu System Architecture) - 2026.01.01
**본 프로젝트는 그누보드 기본 메뉴 테이블(`g5_menu`)을 사용하지 않고, 독립적인 `Pro Menu Manager` 시스템을 사용합니다.**

### A. 핵심 데이터 구조 (Core Table Structure)
*   **테이블명:** `g5_write_menu_pdc` (독립 테이블)
*   **필드 구성:**
    *   `ma_id`: 고유 ID (Auto Increment)
    *   `ma_code`: **계층 구조 식별자** (시스템 자동 생성/관리). 예: `10`(1차), `1010`(2차), `101010`(3차). **사용자가 수정하는 값이 아님.**
    *   `ma_name`: 메뉴명 (예: 회사소개)
    *   `ma_link`: **이동할 실제 URL 또는 식별값**. (예: `1111`, `/theme/about.php`). 메뉴 클릭 시 이 값으로 이동함.

### B. 운영 방식 (Operation Strategy)
1.  **독립 운영:** 테마의 `head.php`는 기본 `g5_menu`가 아닌 `g5_write_menu_pdc` 테이블을 조회하여 메뉴를 구성함.
2.  **3단계(Depth) 지원:** 기본 그누보드(2단계)의 한계를 넘어 3단계 이상의 깊이를 지원하기 위해 `ma_code` 길이 기반의 계층 로직을 사용.
3.  **데이터 매핑:** 프론트엔드 출력을 위해 `lib.php`에서 `ma_` 컬럼들을 표준 `$menu_datas`(`me_name`, `me_link`...) 포맷으로 변환하여 스킨에 전달함.

### C. 사용자 입력 주의사항
*   **코드(Code) vs 링크(Link):**
    *   관리자 화면의 두 번째 입력칸은 **`Link` (이동 경로)**입니다.
    *   여기에 `1111` 등의 테스트 값을 넣으면 해당 주소로 이동하게 됩니다.
    *   구조를 결정하는 **`Code`는 내부적으로 자동 관리**되므로 사용자가 신경 쓸 필요가 없습니다.

---

## 22. 통합 이미지 라이브러리 관리 정책 (Unified Image Library Policy) - 2026.01.02
**사이트 전체의 이미지 자산을 효율적으로 관리하고 재사용하기 위한 통합 저장소 운영 원칙입니다.**

### A. 공용 저장소 사용 원칙 (Shared Repository)
*   **원칙:** 모든 플러그인(메인 관리, 회사소개 등)과 테마에서 업로드하는 이미지는 개별 폴더가 아닌 **`data/common_assets/`** 폴더에 통합 저장한다.
*   **이유:** 
    1.  **재사용성:** 한 번 업로드한 이미지를 다른 메뉴나 섹션에서 다시 올릴 필요 없이 즉시 선택 가능.
    2.  **효율성:** 서버 용량 낭비를 줄이고, 이미지 자산의 체계적인 관리가 가능.

### B. 통합 이미지 매니저 (Integrated Manager)
*   **구조:** [내 컴 업로드 / 라이브러리 / Unsplash] 3단 탭 체제를 표준으로 사용한다.
*   **로직:** 
    1.  업로드 시에는 반드시 **해당 영역의 권장 비율에 맞춘 크롭(Crop)** 기능을 제공해야 한다.
    2.  라이브러리 탭은 항상 `common_assets`의 최신 파일을 목록으로 보여줘야 한다.
    3.  모든 이미지 경로는 `G5_DATA_URL . '/common_assets/'`를 기준으로 처리한다.

### C. 기존 데이터 마이그레이션 필수
*   새로운 플러그인을 개발하거나 연동할 때, 기존 개별 폴더에 이미지가 있다면 반드시 `common_assets`로 이전(Migration)하고 경로를 수정하여 "단일 라이브러리" 원칙을 유지해야 한다.

---

## 23. 통합 버튼 디자인 시스템 (Unified Button System) - 2026.01.02
**사이트 전체의 Call-to-Action(CTA) 버튼의 일관성을 유지하고 중앙 제어하기 위한 원칙입니다.**

### A. 공통 클래스 사용 (Global Class)
*   **원칙:** 모든 플러그인과 테마의 버튼은 개별 스타일이 아닌 **`.mc-btn`** 공통 클래스를 상속받아야 한다.
*   **구구조 (Standardized):**
    1.  `.mc-btn-outline`: 메인 섹션의 **표준 스타일**. 세련된 매거진 느낌을 극대화한다.
    2.  `.mc-btn-primary`: 특수한 강조가 필요한 경우에만 한정적으로 사용한다. (기본은 Outline 권장)

### B. 중앙 제어 (Centralized Control)
*   **위치:** `theme/corporate_light/css/default.css` 내의 `:root` 변수를 통해 제어한다.
*   **변수:** `--mc-btn-padding`, `--mc-btn-weight`, `--mc-btn-letter-spacing` 등을 통해 버튼의 '숨통(Rhythm)'을 사이트 전체에서 일괄 조정한다.


---

## 24. 하드코딩 지양 및 동적 스타일링 원칙 (Anti-Hardcoding & Dynamic Styling) - 2026.01.03
**글로벌 코어 파일이나 공유 컴포넌트에 특정 테마의 전용 스타일을 하드코딩하지 않기 위한 지침입니다.**

### A. 특정 테마 CSS 고정 금지 (No Theme Locking)
*   **문제 발생:** `smart_editor2_inputarea.html` 등의 공통 템플릿 파일에 특정 테마(예: `corporate`)의 CSS를 직접 연결하면, 다른 테마(라이트 테마 등) 사용 시 배경색 충돌이나 디자인 깨짐이 발생합니다.
*   **원칙:** 글로벌 코어 파일은 '순정(Pure)' 상태를 유지해야 하며, 특정 테마의 CSS에 종속되어서는 안 됩니다.

### B. 동적 인젝션(Dynamic Injection) 활용
*   에디터 내부나 공통 영역의 스타일을 테마에 맞춰 변경해야 할 경우, 파일을 직접 수정하는 대신 **자바스크립트를 통한 실시간 주입(Injection)** 방식을 사용합니다.
*   **해결책 예시:** 
    1.  관리자 페이지에서 테마를 인식하여 배경색 등 필요한 변수들을 결정합니다.
    2.  에디터가 로딩된 후, JS를 통해 에디터 iframe 내부의 `body`에 배경색과 CSS 변수(`--cp-bg` 등)를 강제 주입합니다.
*   **장점:** 코어 파일의 수정을 최소화하면서도, 여러 테마와 설정에 따라 유연하게 대응할 수 있습니다.

### C. 실패 사례 분석 (Lesson Learned)
*   에디터에서 실제 사이트의 다크 감성을 보고 싶다는 이유로 `SmartEditor2` 내부 HTML에 `theme/corporate/css/default.css`를 직접 링크한 결과, 라이트 테마 작업 시에도 에디터가 검게 나오는 오류가 발생했습니다. (2025.12.28 수정 이력 확인됨)
*   **교훈:** '잠깐의 편리함'을 위해 코어 파일을 특정 테마에 묶어버리는 행위는 나중에 반드시 더 큰 유지보수 비용(버그)으로 돌아옵니다.


## 25. 관리자 목록 삭제 패턴 (List-based Deletion Pattern) - 2026.01.05
**`update.php`를 거치지 않고 목록 페이지에서 즉시 삭제하여 보안 토큰 오류를 우회하는 패턴입니다.**

### A. 문제 상황
*   **증상:** 관리자 목록에서 '삭제' 버튼 클릭 시 `delete_confirm(this)` 함수가 실행되고, 이후 `update.php` 로 이동하며 "토큰 정보가 올바르지 않습니다" 오류 발생.
*   **원인 1 (Server):** `check_admin_token()` 검증 시 세션 불일치 발생.
*   **원인 2 (Client):** `adm/admin.js`의 `delete_confirm()` 함수가 `href`에 토큰을 강제로 주입하거나, 폼 전송 방식으로 동작을 변경하여 문제를 야기함.

### B. 해결 방법
*   **원칙:** `delete_confirm()` 함수를 사용하지 않고, 순수 `confirm()` 함수와 `list.php` 직접 처리를 사용한다.
*   **코드 예시 (`list.php` - Server):**
    ```php
    // 목록 쿼리 실행 전 최상단에 배치
    if (isset($_GET['w']) && $_GET['w'] == 'd' && isset($_GET['tm_id'])) {
        // 1. ID 정제 (SQL Injection 방지)
        $tm_id = preg_replace('/[^a-zA-Z0-9_]/', '', $_GET['tm_id']);
        
        // 2. 파일 삭제 및 DB 삭제
        $row = sql_fetch(" SELECT * FROM ... WHERE tm_id = '{$tm_id}' ");
        @unlink(...);
        sql_query(" DELETE FROM ... WHERE tm_id = '{$tm_id}' ");
        
        // 3. 리로딩 (update.php로 가지 않음)
        alert('삭제되었습니다.', './list.php');
    }
    ```
*   **링크 수정 (`list.php` - Client):**
    *   `href`를 `list.php?w=d...`로 변경.
    *   `onclick` 이벤트를 `return confirm(...)`으로 단순화.
    ```html
    <!-- [BAD] delete_confirm 사용 시 admin.js가 간섭함 -->
    <a href="./update.php?..." onclick="return delete_confirm(this);">삭제</a>

    <!-- [GOOD] 순수 confirm 사용 및 list.php로 직접 이동 -->
    <a href="./list.php?w=d&tm_id=..." onclick="return confirm('정말 삭제하시겠습니까?');">삭제</a>
    ```

